// All of this is done in the terminal.
// Git Basics: (Super common Git commands);
// Non Git Commands:
// mkdir (name of your new directory)
// cd (name of a child directory of the one you are currently in)
// code (name of a new file)
// dir (shows the non hidden files in your directory)
// dir -Hidden (Shows only the hidden files in your directory)

// Git Commands
/*
git init
^(This will make a new repository and give you the path.)

git status
^(This will tell you the state of your directory)

git add .
^(This will add all untracked files)

git add (path)
^(This will add everything inside of the directory.)

git commit -m "Initial commit"
^(This will send a commit message (that's what the -m stands for.))
*/