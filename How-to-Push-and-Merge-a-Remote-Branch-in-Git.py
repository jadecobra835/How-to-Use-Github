# This is how we can push a local branch to github.

"""
GIT COMMANDS
git checkout -b another-feature
^(Again, making a new branch)

# Edit a file

# Do a commit

git push -u origin name-of-branch
^(This boils down to "git push upstream remoteorigin name-of-branch)
^^(This is how we can push our local branches to github.)

NOW GOT TO GITHUB AND OPEN A COMPARE AND PULL REQUEST
^(Click all of the green buttons that you are supposed to)
"""

# Miscleaneous Command:
# git branch -d name-of-branch
# ^(This will delete the branch that you called)